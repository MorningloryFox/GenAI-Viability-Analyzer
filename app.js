const form=document.querySelector('#form'),key='decision-studio-simulations',used=()=>Number(localStorage.getItem(key)||0),limit=document.querySelector('#limit'),money=value=>value.toLocaleString('pt-BR',{style:'currency',currency:'BRL',maximumFractionDigits:0});const fields=[['annualCost','costOut',money],['automationRate','rateOut',value=>`${value}%`],['implementationCost','implOut',money]];fields.forEach(([name,id,format])=>{const input=form.elements[name],output=document.querySelector(`#${id}`);input.oninput=()=>output.value=format(Number(input.value))});const renderLimit=()=>limit.textContent=`${Math.max(0,5-used())} de 5 simulações restantes neste navegador.`;renderLimit();form.onsubmit=async event=>{event.preventDefault();if(used()>=5){limit.textContent='Limite da demonstração atingido neste navegador.';return}const button=form.querySelector('button');button.disabled=true;button.textContent='Calculando cenário…';try{const payload=Object.fromEntries(new FormData(form));const response=await fetch('/.netlify/functions/assess',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(payload)});const data=await response.json();if(!response.ok)throw new Error(data.error);localStorage.setItem(key,String(used()+1));document.querySelector('#recommendation').textContent=data.recommendation;document.querySelector('#roi').textContent=`${data.roi}%`;document.querySelector('#payback').textContent=data.payback?`${data.payback} meses`:'—';document.querySelector('#readiness').textContent=`${data.readiness}%`;document.querySelector('#gross').textContent=money(data.gross);document.querySelector('#run').textContent=money(data.annualRun);document.querySelector('#riskLabel').textContent=data.riskLabel;document.querySelector('#narrative').textContent=data.narrative;renderLimit()}catch(error){limit.textContent=error.message}finally{button.disabled=false;button.innerHTML='Simular decisão <span>↗</span>'}};
